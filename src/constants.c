#include <stdio.h>

int main(){

    // constant = a value that is fixed, so it can't be changed if not directly in the variable in the code
    // it's common practice to name all constants's names in uppercase

    const float PI = 3.14159;

    //pi = 420.63;

    return 0;
}